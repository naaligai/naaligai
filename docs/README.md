# Sphinx with rtd_theme documentation

Run `make` command inside `/docs/` path. It shows help for documenting documentation.

run `make html` to produce html documentation.

Then activate live server in VS Code editor, path is `docs/_build/html/index.html`
